# Navy Thunder 路线图（Phase 7 之后）

基线：Phase 0–7 全部完成（commit `0f9e465`），93 测试全绿，校准报告 12/12。
本路线图覆盖五条工作流，按优先级排列；每项给出依赖、验收标准与规模（S<1 天 / M≈1-3 天 / L>3 天）。

## W1 引擎闭环 —— 让"战列舰对决"真正跑起来（最高优先级）

计划 Phase 3 的验收场景目前由测试内部接线（`CombatHarness.FireShellAt`）达成，
尚缺一条从场景文件到战斗报告的完整命令链。

| 项 | 内容 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| W1.1 舰炮武器系统 | 炮塔开火系统：装填循环（当前已有 `CurrentReloadFactor`）驱动 `ConsumeReadyRack`、齐射模式、散布→弹道生成，闭合"瞄准→开火→弹药消耗→补给"链 | 无 | 一个炮塔在场景中按射速持续开火且弹药账目正确 | M |
| W1.2 SimRunner 场景格式 v1 | `scenario { ships[], mounts[], firePlan[] }` JSON → 确定性战斗 → CSV/JSON 报告（命中数/毁伤时间线/击沉原因） | W1.1 | `nt-sim --scenario bb_duel.json` 输出一次战列舰对决的完整战报，重跑逐字节一致 | M |
| W1.3 TDS 衰减层 | 防雷凸起=几何+隔舱衰减层（吸收/削减水爆），不拦截水下 AP（MDR-0012 已留接口） | 无 | 带 TDS 舰受到的进水/伤害低于无 TDS 对照 | S |
| W1.4 飞行模型插件 | 简化气动积分器消费 `FlightFeedback`（升力/阻力/推力/操纵权限），使飞机真实飞行、被击伤后性能衰减、超 G 断翼自然发生 | 无 | 受损飞机（断缆/毁翼）在积分器中表现出计划 B4 的飞行性能衰减 | M |
| W1.5 Protection Analysis 全链路 | `scenario --target <shipId> --hit "belt,frame45"` → 模块/舰员/火灾/进水/最终状态报告（计划 §25 的完整形态） | W1.1 | 对 test_battleship 输出含舰员伤亡与模块状态的命中报告 | M |

## W2 校准深化 —— 从"边界正确"到"数值可信"

| 项 | 内容 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| W2.1 转正乘数表拟合 | 现有 30°/60° 门限只验证"可穿透边界"；收集 WT Protection Analysis 记录案例（弹×板×角度结果），按弹种拟合乘数表进 calibration | W1.5 | 角度测试从上下界升级为 ±15% 数值门 | M |
| W2.2 阻力曲线精化 | Mk13 HE 表 ±12% 偏松；用 LAW_1943 的 Cd(M) 折线替换单一 `DragCoefficientScale`（接口已预留） | 无 | Mk13 全距离 ±8% 以内 | M |
| W2.3 行为数值校准 | 破片数/初速、起火概率、燃烧 DPS、进水流量、DC 系数 —— 全部是 approximation 默认值；对照社区实测记录逐项拟合（起火烧死舱员时间、进水倾覆时间等行为锚点） | W1.2 | 每项在 MDR 中有"实测锚点 → 拟合值"记录 | L |
| W2.4 校准报告进 CI | `nt-pa report` 在 CI 中运行并作为合并门（12/12 回归即失败） | W4.1 | CI 红绿灯工作 | S |

## W3 数据扩录 —— 管线已就绪的持续数据流

分级约定（沿用 README）：Tier-1 精校 / Tier-2 模板拟合 / Tier-3 占位。

| 项 | 内容 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| W3.1 机炮 K 值替换 | `extract_datamine.py` 跑全量 aircraft_weapons，把 .50/20mm 等拟合 K 替换为 datamine 实值 | 网络（raw.githubusercontent 间歇可用，支持 `--dir` 本地 datamine） | air_guns.json 来源字段从 hand_authored 变为 wt_datamine | S |
| W3.2 舰炮弹药扩录 | 目标 ≥150 种：extractor 批量 + 无 datamine 条目用 Navweaps 史实参数（Tier-2 标注） | W3.1 同网络条件 | 数据集计数 + 全量 schema 校验绿 | M |
| W3.3 飞机扩录 ≥15 机 | 复用 generate_ships 思路：机型参数表（重量/翼展/G 限/部件布局模板）→ 生成器 | 无 | 15 机型过校验，前 5 机型手工精修 | M |
| W3.4 导弹参数表 ≥40 | IR/SARH/ARH 逐弹参数（FOV/过载/引信半径），公开来源 Tier-2 | 无 | 40 弹过校验，IR/SARH/ARH 各 ≥8 | M |
| W3.5 舰船布局精修 | 前 10 艘人气舰：舷侧分舱（左/右）、TDS 隔舱、炮塔布局按史实剖面调整（当前为中线对称模板） | W1.3 | 精修舰的鱼雷/HE 行为与模板舰出现可测差异 | L |

## W4 工程化 —— 保护既有成果

| 项 | 内容 | 依赖 | 验收 | 规模 |
|---|---|---|---|---|
| W4.1 GitHub Actions CI | push/PR：build + 93 测试 + 数据 schema 校验 + `nt-pa report` 门槛 | 无 | 绿勾徽章；校准回归自动红 | S |
| W4.2 性能预算测试 | 计划风险 E4：AA 弹幕（数千弹丸）+ 破片云的每 tick 耗时上限断言；弹丸/破片对象池 | W1.1 | 基准测试进 CI，超预算即红 | M |
| W4.3 确定性回归文件 | 黄金文件机制（矩阵输出快照）接 CI，防止无意的行为漂移 | W1.2 | 黄金 diff 即 PR 可见 | S |

## W5 后续大项（需要时再启动）

- **Unity 前端（Phase 8）**：Core 零依赖设计已预留；启动条件 = W1/W2 稳定后。
- **NavalArt 本地数据**（`D:\SteamLibrary\steamapps\common\NavalArt`）：建造体系参考（部件清单/布局习惯），服务于前端阶段的建造器数据模型；战斗数值不采用（计划既定）。
- **网络同步**：确定性 tick + 事件日志已是 lockstep 就绪形态，无前置工作。

## 建议执行顺序

```
W4.1 CI → W1.1 武器系统 → W1.2 场景格式 ─┬→ W2.4 报告门槛
                                          ├→ W2.1/W2.3 数值校准
                                          └→ W1.5 PA 全链路
W3.1（网络可用时穿插）→ W3.2/W3.3/W3.4 数据扩录（持续并行）
W1.3 → W1.4 → W3.5 精修
```

前两周产出物：CI 绿灯 + 一次完整 AI 驱动的战列舰对决战报 + 转正乘数表首个拟合版本。
